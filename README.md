# Installation

Make sure you have the latest version of Node.js LTS

open the terminal in the app's directory then run the following command

```sh
npm install
```

# Usage

copy configuration file .env.example to .env and open it to add your config in.

`cp .env.example .env`

run the seed command to intialize the database.

`npm run seed`

then run the start command to start the server locally at port

`npm start`